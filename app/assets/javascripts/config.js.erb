"use strict";

angular.module('bastet.constants')
  .constant('config', {
    is_development: <%= Rails.env.development? %>,
    websockets_host: '<%= Rails.application.config.custom[:websockets_host] %>'
  });